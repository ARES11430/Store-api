spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=MySQL
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop # Creates schema for tests, drops after
  flyway:
    enabled: false # Disable flyway for tests to let Hibernate handle schema generation for speed
stripe:
  secretKey: "sk_test_mock"
  webhookSecretKey: "whsec_mock"
websiteUrl: http://localhost:8080
spring.jwt:
  secret: "404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970" # Mock secret for tests
  accessTokenExpiration: 900
  refreshTokenExpiration: 604800